name: Validate Version

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions: 
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for tag comparison

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Upgrade pip
      run: python -m pip install --upgrade pip

    - name: Validate version
      run: |
        # Extract version from pyproject.toml
        PYPROJECT_VERSION=$(python -c "import tomllib; data=tomllib.load(open('pyproject.toml','rb')); print(data['project']['version'])")
        echo "pyproject.toml version: $PYPROJECT_VERSION"
        
        # For push/PR: warning only (don't fail)
        echo "Running on push/PR - performing format check only"
        
        # Check if version follows semantic versioning
        if ! echo "$PYPROJECT_VERSION" | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' > /dev/null; then
          echo "⚠️  WARNING: Version '$PYPROJECT_VERSION' does not follow semantic versioning (X.Y.Z)"
        else
          echo "✓ Version format check passed!"
        fi
        
        # Check if version matches latest tag
        LATEST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "no-tags")
        LATEST_TAG=${LATEST_TAG#v}
        
        if [ "$LATEST_TAG" != "no-tags" ]; then
          echo "Latest git tag: $LATEST_TAG"
          if [ "$PYPROJECT_VERSION" == "$LATEST_TAG" ]; then
            echo "⚠️  WARNING: pyproject.toml version matches latest tag - remember to bump version for next release!"
          fi
        fi
